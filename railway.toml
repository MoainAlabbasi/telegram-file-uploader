[build]
builder = "nixpacks"
buildCommand = "npm install && python3 -m venv venv && source venv/bin/activate && pip install python-docx openpyxl fpdf2 markdown"

[deploy]
startCommand = "node server.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

[env]
NODE_ENV = "production"
