# 📦 دليل التثبيت والتشغيل - MoTech Cloud v4.0

## 🎯 نظرة عامة

هذا الدليل يشرح كيفية تثبيت وتشغيل النظام المحدث مع جميع الميزات الجديدة:
- ✅ نظام إدارة الاختبارات (يدوي وبالذكاء الاصطناعي)
- ✅ عارض محتوى متدفق مع واجهة مخصصة
- ✅ تصدير بصيغ متعددة (DOCX, PDF, Excel, PowerPoint, TXT, MD)
- ✅ تحديثات شاملة لميزة الملخصات

---

## 📋 المتطلبات الأساسية

### 1. Node.js
```bash
node --version  # يجب أن يكون >= 18.0.0
```

### 2. Python 3
```bash
python3 --version  # يجب أن يكون >= 3.8
```

### 3. Pandoc (اختياري لكن موصى به للتصدير المتقدم)
```bash
# Ubuntu/Debian
sudo apt-get install pandoc

# macOS
brew install pandoc

# التحقق
pandoc --version
```

### 4. قاعدة بيانات Supabase
- حساب على [Supabase](https://supabase.com)
- مشروع جديد أو موجود

### 5. Telegram Bot
- Bot Token من [@BotFather](https://t.me/BotFather)
- Chat ID للقناة أو المجموعة

### 6. Google Gemini API Key
- مفتاح API من [Google AI Studio](https://makersuite.google.com/app/apikey)

---

## 🚀 خطوات التثبيت

### الخطوة 1: استنساخ المشروع
```bash
git clone https://github.com/MoainAlabbasi/telegram-file-uploader.git
cd telegram-file-uploader
```

### الخطوة 2: تثبيت تبعيات Node.js
```bash
npm install
```

### الخطوة 3: إعداد البيئة الافتراضية لـ Python
```bash
python3 -m venv venv
source venv/bin/activate  # على Linux/Mac
# أو
venv\Scripts\activate  # على Windows

pip install python-docx openpyxl fpdf2 markdown
```

### الخطوة 4: تحديث قاعدة البيانات
قم بتشغيل السكربتات التالية بالترتيب في Supabase SQL Editor:

1. **إذا كانت قاعدة بيانات جديدة:**
   ```bash
   # نفذ محتوى الملف:
   supabase-setup.sql
   ```

2. **ثم نفذ التحديثات الجديدة:**
   ```bash
   # نفذ محتوى الملف:
   QUIZ_SYSTEM_UPDATE.sql
   ```

### الخطوة 5: إعداد المتغيرات البيئية
أنشئ ملف `.env` في جذر المشروع:

```env
# Telegram Configuration
BOT_TOKEN=your_telegram_bot_token_here
CHAT_ID=your_chat_id_here

# Supabase Configuration
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your_supabase_anon_key_here

# Google Gemini AI
GEMINI_API_KEY=your_gemini_api_key_here

# Server Configuration
PORT=3000
NODE_ENV=production
```

### الخطوة 6: التشغيل

#### للتطوير:
```bash
npm run dev
```

#### للإنتاج:
```bash
npm start
```

سيعمل السيرفر على: `http://localhost:3000`

---

## 🗂️ بنية المشروع المحدثة

```
telegram-file-uploader/
├── server.js                    # السيرفر الرئيسي (محدث)
├── supabase.js                  # دوال قاعدة البيانات (محدث)
├── converter.py                 # محول الصيغ (جديد/محدث)
├── package.json
├── requirements.txt             # تبعيات Python (جديد)
├── .env                         # المتغيرات البيئية
│
├── public/                      # الواجهات الأمامية
│   ├── index.html              # الصفحة الرئيسية
│   ├── study.html              # المساعد الدراسي (محدث)
│   ├── gallery.html            # معرض الملفات
│   ├── quizzes.html            # عرض الاختبارات (جديد)
│   ├── summaries.html          # عرض الملخصات (جديد)
│   ├── quiz-creator.html       # منشئ الاختبارات (جديد)
│   └── viewer.html             # العارض المتدفق (جديد)
│
├── prompts/                     # موجهات الذكاء الاصطناعي
│   ├── summary.js              # موجه الملخصات
│   └── quiz.js                 # موجه الاختبارات
│
├── venv/                        # البيئة الافتراضية لـ Python
│
└── sql/                         # سكربتات قاعدة البيانات
    ├── supabase-setup.sql      # الإعداد الأساسي
    └── QUIZ_SYSTEM_UPDATE.sql  # تحديثات الاختبارات (جديد)
```

---

## 🎨 الميزات الجديدة

### 1. نظام الاختبارات 📚

#### إنشاء يدوي:
- واجهة تفاعلية لإنشاء الاختبارات
- دعم أنواع متعددة من الأسئلة:
  - اختيار متعدد
  - صواب/خطأ
  - إكمال الفراغ
  - تعريف
  - مطابقة
  - ترتيب
- تحديد الدرجات والشروحات لكل سؤال

#### إنشاء بالذكاء الاصطناعي:
- توليد اختبارات تلقائياً من الملفات
- تخصيص عدد الأسئلة ونوعها
- استخدام Gemini 2.0 Flash

### 2. العارض المتدفق 🎬
- عرض محتوى Markdown بتنسيق جميل
- دعم كامل للعربية
- واجهة مستخدم احترافية
- شريط أدوات للتصدير

### 3. التصدير المتعدد 📤
- DOCX (Microsoft Word)
- PDF (مستندات محمولة)
- XLSX (Excel)
- PPTX (PowerPoint)
- TXT (نص عادي)
- MD (Markdown)

### 4. تحديثات الملخصات 📝
- صفحة مخصصة لعرض جميع الملخصات
- إحصائيات متقدمة
- بحث وفلترة
- عرض متدفق مع التصدير

---

## 🔧 استكشاف الأخطاء

### مشكلة: فشل التحويل إلى PDF
**الحل:** تثبيت pandoc والمكتبات المطلوبة:
```bash
sudo apt-get install pandoc texlive-xetex
```

### مشكلة: خطأ في استيراد المكتبات Python
**الحل:** تأكد من تفعيل البيئة الافتراضية:
```bash
source venv/bin/activate
pip install -r requirements.txt
```

### مشكلة: فشل الاتصال بـ Supabase
**الحل:** تحقق من:
- صحة SUPABASE_URL و SUPABASE_KEY
- تشغيل سكربتات SQL
- صلاحيات الـ API Key

### مشكلة: فشل الذكاء الاصطناعي
**الحل:** تحقق من:
- صحة GEMINI_API_KEY
- الحصة المتاحة في Google AI
- صيغة الملف المدعومة

---

## 📊 API Endpoints الجديدة

### الاختبارات
```
POST   /api/quiz/create          # إنشاء اختبار يدوي
POST   /api/quiz/generate-ai     # إنشاء اختبار بالذكاء الاصطناعي
GET    /api/quizzes              # الحصول على جميع الاختبارات
GET    /api/quiz/:id             # الحصول على اختبار محدد
GET    /api/quiz/content/:id     # الحصول على محتوى اختبار (Streaming)
DELETE /api/quiz/:id             # حذف اختبار
GET    /api/quizzes/search?q=    # البحث في الاختبارات
```

### الملخصات
```
GET    /api/summaries            # الحصول على جميع الملخصات
GET    /api/summary/content/:id  # الحصول على محتوى ملخص (Streaming)
DELETE /api/summary/:id          # حذف ملخص
GET    /api/summaries/search?q=  # البحث في الملخصات
```

### التصدير
```
POST   /api/export               # تصدير محتوى بصيغة محددة
Body: {
  content: "markdown content",
  format: "docx|pdf|xlsx|pptx|txt|md",
  filename: "filename"
}
```

### الإحصائيات
```
GET    /api/stats/quizzes        # إحصائيات الاختبارات
GET    /api/stats/summaries      # إحصائيات الملخصات
GET    /api/stats/system         # إحصائيات النظام
```

---

## 🌐 النشر (Deployment)

### Railway
1. ربط المشروع بـ GitHub
2. إضافة المتغيرات البيئية
3. إضافة buildpack لـ Python إذا لزم الأمر
4. النشر تلقائياً

### Heroku
```bash
heroku create your-app-name
heroku config:set BOT_TOKEN=xxx CHAT_ID=xxx ...
git push heroku main
```

### VPS (Ubuntu)
```bash
# تثبيت Node.js و Python
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs python3 python3-pip pandoc

# استنساخ المشروع
git clone https://github.com/MoainAlabbasi/telegram-file-uploader.git
cd telegram-file-uploader

# التثبيت
npm install
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# إعداد .env
nano .env

# تشغيل باستخدام PM2
npm install -g pm2
pm2 start server.js --name motech-cloud
pm2 save
pm2 startup
```

---

## 📞 الدعم

إذا واجهت أي مشاكل:
1. تحقق من ملف `ARCHITECTURE_DESIGN.md` للتفاصيل التقنية
2. راجع الـ logs في الكونسول
3. تأكد من تشغيل جميع الخدمات المطلوبة

---

## 📝 ملاحظات مهمة

1. **الأمان:** لا تشارك ملف `.env` أبداً
2. **النسخ الاحتياطي:** احتفظ بنسخة احتياطية من قاعدة البيانات
3. **التحديثات:** راجع التحديثات بانتظام على GitHub
4. **الأداء:** استخدم CDN للملفات الثابتة في الإنتاج

---

## ✨ ما الجديد في v4.0

- ✅ نظام اختبارات كامل (يدوي + AI)
- ✅ عارض محتوى متدفق احترافي
- ✅ تصدير بـ 6 صيغ مختلفة
- ✅ صفحات مخصصة للملخصات والاختبارات
- ✅ إحصائيات متقدمة
- ✅ بحث وفلترة محسّنة
- ✅ دعم كامل للعربية في جميع الميزات
- ✅ تحسينات في الأداء والأمان

---

**تم التطوير بواسطة:** MoTech Team  
**الإصدار:** 4.0  
**التاريخ:** نوفمبر 2025
